# Netlify redirects configuration for SEO optimization

# Force HTTPS redirect (301 permanent redirect)
http://iplmanufacturer.com/* https://iplmanufacturer.com/:splat 301!
http://www.iplmanufacturer.com/* https://www.iplmanufacturer.com/:splat 301!

# Canonical domain redirect (www to non-www or vice versa)
# Choose one: either redirect www to non-www OR non-www to www
# Option 1: Redirect www to non-www (recommended for better SEO)
https://www.iplmanufacturer.com/* https://iplmanufacturer.com/:splat 301!

# Option 2: If you prefer www version, use this instead:
# https://iplmanufacturer.com/* https://www.iplmanufacturer.com/:splat 301!

# Language-specific redirects for SEO
# Redirect old language URLs to new structure if needed
/en/* /:splat 301
/english/* /:splat 301

# Product page redirects for better URL structure
/products/* /ipl-hair-removal/:splat 301
/devices/* /ipl-hair-removal/:splat 301

# Service page redirects
/services/* /service-support/:splat 301
/support/* /service-support/:splat 301

# Blog redirects
/news/* /blog/:splat 301
/articles/* /blog/:splat 301

# Trailing slash handling
/*/ /:splat 301

# SPA fallback for client-side routing
/* /index.html 200